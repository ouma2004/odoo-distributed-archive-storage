# .env — variables utilisées par docker-compose.yml
#
# Copier en `.env` puis renseigner. 
#

# ── PostgreSQL / base Odoo ────────────────────────────────────────────
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
